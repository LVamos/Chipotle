namespace Game.Controls.DualSense
{
	/// <summary>
	/// Directions for the D-pad on the DualSense controller.
	/// </summary>
	public enum DualSenseDPad
	{
		DPadUp,
		DPadDown,
		DPadLeft,
		DPadRight
	}
}